public with sharing class ContentVersionTriggerHandler {
    // public static void afterInsert(List<ContentVersion> newVersions) {
    //     for (ContentVersion version : newVersions) {
    //         try {
    //             // Call the WhisperAIService to process the audio file
    //             WhisperAIService.processAudioFile(version.ContentDocumentId);
    //         } catch (Exception ex) {
    //             // Log or handle the error as necessary
    //             System.debug('Error during transcription process: ' + ex.getMessage());
    //         }
    //     }
    // }
}
